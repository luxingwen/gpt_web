import React, { useState } from 'react';

import '../index.less';

interface TerasureBoxProps {}
const TerasureBox: React.FC<TerasureBoxProps> = ({}) => {
  const [headerState, setHeaderState] = useState<number>(0);

  return <>AI百宝助手</>;
};

export default TerasureBox;
